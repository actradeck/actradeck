{
  "name": "@actradeck/telemetry-collector",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Privacy-minimal Cloudflare Worker and D1 aggregate PMF API for explicit ActraDeck opt-in telemetry.",
  "scripts": {
    "build:worker": "WRANGLER_SEND_METRICS=false wrangler deploy --dry-run --config wrangler.example.jsonc --outdir dist",
    "type-check": "tsc --noEmit",
    "test:worker": "WRANGLER_SEND_METRICS=false vitest run",
    "dev": "WRANGLER_SEND_METRICS=false wrangler dev --config wrangler.local.jsonc",
    "deploy": "WRANGLER_SEND_METRICS=false wrangler deploy --config wrangler.local.jsonc",
    "db:migrate:local": "WRANGLER_SEND_METRICS=false wrangler d1 migrations apply DB --local --config wrangler.local.jsonc",
    "db:migrate:remote": "WRANGLER_SEND_METRICS=false wrangler d1 migrations apply DB --remote --config wrangler.local.jsonc"
  },
  "dependencies": {
    "@actradeck/telemetry-contract": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.21.2",
    "@cloudflare/workers-types": "5.20260813.1",
    "typescript": "6.0.3",
    "vitest": "4.1.8",
    "wrangler": "4.122.0"
  }
}
