{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "composite": true,
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    // token-equals.ts が node:crypto/Buffer を使うため (design-tokens と同パターン)。
    "types": ["node"]
  },
  "references": [{ "path": "../event-model" }],
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules", "src/**/*.test.ts"]
}
